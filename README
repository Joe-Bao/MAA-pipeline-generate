## 使用

1. 准备模板 `template.jsonc`：用 `${Var}` 占位符描述任务结构
2. 准备数据 `data.json`：
   - 纯数组：`[{ "Id": "xxx", "Name": "xxx", ... }, ...]`
   - 或带配置：`{ "outputPattern": "${Id}.json", "data": [...] }`
3. 执行：`node generate.mjs`
4. 输出在 `output/` 目录

## 选项

node generate.mjs [--template path] [--data path] [--output-dir path] [--output-pattern pattern] [--merged]